/**
 * An action with a type property and any number of other properties to be used by a reducer.
 * @typedef {Object.<String,any>} Action
 * @property {string} type
 */

/**
 * The object that represents global state for the elder scrolls viewer
 * @typedef {Object} GlobalState
 * @property {Array.<{displayName: string, apiParameter: string, values: Array.<string>}>} filters
 * @property {Array<ElderScrollsAPICardsPage>} cardsPages
 * @property {boolean} isDonePaging
 */
